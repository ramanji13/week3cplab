#include <stdio.h>

int main() {
    int a,b,c,d,e;
    int max=a;
    scanf("%d%d%d%d%d",&a,&b,&c,&d,&e);
    if(max<b){
        max=b;
    
    if(max<c)
        max=c;
        
    if (max<d)
        max=d;
        
    if (max<e)
        max=e;
    
    printf("\n%d is big\n",max);
    }
}
